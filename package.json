{
  "name": "bettingInTheIPAT",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "zip": "rm -rf node_modules && npm install --production && if [ -e index.zip ]; then rm index.zip ; fi && zip -r index.zip *",
    "lint": "eslint index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@serverless-chrome/lambda": "^1.0.0-55",
    "chrome-remote-interface": "^0.26.1",
    "puppeteer": "^2.1.1"
  },
  "devDependencies": {
    "aws-sdk": "^2.327.0",
    "chrome-launcher": "^0.13.2"
  }
}
